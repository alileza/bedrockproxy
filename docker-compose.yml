services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bedrockproxy
      POSTGRES_PASSWORD: bedrockproxy
      POSTGRES_DB: bedrockproxy
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
